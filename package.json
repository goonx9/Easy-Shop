{
  "name": "easyshop-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp index.html scanner.html dist/",
    "preview": "node server.js",
    "clean": "rm -rf dist",
    "lint": "echo 'No lint needed for pure HTML'"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
